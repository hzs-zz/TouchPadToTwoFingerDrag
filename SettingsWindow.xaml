<ui:FluentWindow x:Name="wadpy_pn_RootWindow"
        x:Class="TouchpadToMiddleClick.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="拯救windows的垃圾触摸板适配！" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" 
        ExtendsContentIntoTitleBar="True" 
        WindowBackdropType="Mica"
        Foreground="Black"
        ResizeMode="CanMinimize">

    <ui:FluentWindow.Resources>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" Background="Transparent" SnapsToDevicePixels="true">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False" Opacity="0" Height="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="{x:Type Thumb}">
                                                <Setter Property="OverridesDefaultStyle" Value="true" />
                                                <Setter Property="IsTabStop" Value="false" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                                            <Rectangle Fill="#33000000" RadiusX="4" RadiusY="4" Width="6" />
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False" Opacity="0" Height="0" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="拯救windows的垃圾触摸板适配！" Padding="10,0" />

        <TabControl Grid.Row="1" Margin="5,10,5,5" TabStripPlacement="Left" Background="Transparent" BorderThickness="0">

            <TabItem Header="中键平移" FontSize="15" Margin="0,0,0,8">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{x:Null}">
                    <StackPanel Margin="25">

                        <ui:Card x:Name="PanCard" Padding="15" Margin="0,0,0,20" Cursor="Hand" MouseLeftButtonDown="PanCard_MouseLeftButtonDown">
                            <ui:Card.Style>
                                <Style TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
                                    <Setter Property="Background" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=PanSwitch}" Value="True">
                                            <Setter Property="Background" Value="#005FB8"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Card.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="中键接管总开关（alt+shift+m）" FontSize="18" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, ElementName=PanSwitch}" Value="True">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="开启后，在目标窗口内滑动将转为中键平移" Margin="0,4,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#88000000"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, ElementName=PanSwitch}" Value="True">
                                                        <Setter Property="Foreground" Value="#DDFFFFFF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                                <ui:ToggleSwitch x:Name="PanSwitch" Grid.Column="1" VerticalAlignment="Center"/>
                            </Grid>
                        </ui:Card>

                        <TextBlock Text="中键平移进程名单" FontWeight="SemiBold" Margin="5,0,0,10"/>
                        <ui:Card Padding="5">
                            <ListBox ItemsSource="{Binding PanProcesses}" MinHeight="100" Background="Transparent" BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Expander Header="{Binding ProcessName}" Background="#05000000" BorderThickness="0" Margin="0,0,0,5" Padding="10">
                                            <StackPanel Margin="30,10,10,10">

                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" Text="生效视口类名:" Foreground="#88000000" VerticalAlignment="Center"/>

                                                    <ui:Card Grid.Column="2" Padding="8,4" Margin="0,0,10,0" Cursor="Hand" MouseLeftButtonDown="ReverseRuleCard_MouseLeftButtonDown">
                                                        <ui:Card.Style>
                                                            <Style TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
                                                                <Setter Property="Background" Value="White"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ReverseSwitch}" Value="True">
                                                                        <Setter Property="Background" Value="#FF8C00"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </ui:Card.Style>
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <TextBlock Text="反向规则 (黑名单)" VerticalAlignment="Center" Margin="0,0,8,0">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="Black"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsChecked, ElementName=ReverseSwitch}" Value="True">
                                                                                <Setter Property="Foreground" Value="White"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                            <ui:ToggleSwitch x:Name="ReverseSwitch" IsChecked="{Binding IsReverseRule, Mode=TwoWay}" Checked="ReverseSwitch_Changed" Unchecked="ReverseSwitch_Changed" Margin="0" Padding="0"/>
                                                        </StackPanel>
                                                    </ui:Card>

                                                    <ui:Button Grid.Column="3" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" Content="移除此进程" HorizontalAlignment="Right" Click="RemovePanProcess_Click"/>
                                                </Grid>
                                                <ItemsControl ItemsSource="{Binding TargetClasses}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border BorderBrush="#11000000" BorderThickness="0,0,0,1" Padding="5">
                                                                <Grid>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <ui:SymbolIcon Symbol="Code24" Margin="0,0,8,0" Foreground="#55000000"/>
                                                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" FontFamily="Consolas"/>
                                                                    </StackPanel>
                                                                    <ui:Button Icon="{ui:SymbolIcon Dismiss24}" Appearance="Transparent" HorizontalAlignment="Right" Foreground="#CCFF4444" Padding="4" Click="RemovePanClass_Click" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Expander}}"/>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <Grid Margin="0,15,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ui:TextBox x:Name="PanClassInput" PlaceholderText="输入类名 (如:SysListView32)"/>
                                                    <ui:Button Grid.Column="1" Content="添加类名" Margin="10,0,0,0" Appearance="Primary" Click="AddPanClass_Click" Tag="{Binding ElementName=PanClassInput}" />
                                                </Grid>
                                            </StackPanel>
                                        </Expander>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </ui:Card>
                        <ui:Card Padding="15" Margin="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ui:TextBox x:Name="NewPanProcessInput" PlaceholderText="添加新平移进程 (如: SLDWORKS)"/>
                                <ui:Button Grid.Column="1" Content="添加进程" Margin="10,0,0,0" Appearance="Secondary" Click="AddPanProcess_Click"/>
                            </Grid>
                        </ui:Card>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="滚动模拟" FontSize="15" Margin="0,0,0,8">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{x:Null}">
                    <StackPanel Margin="25">

                        <ui:Card x:Name="ScrollCard" Padding="15" Margin="0,0,0,20" Cursor="Hand" MouseLeftButtonDown="ScrollCard_MouseLeftButtonDown">
                            <ui:Card.Style>
                                <Style TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
                                    <Setter Property="Background" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=ScrollSwitch}" Value="True">
                                            <Setter Property="Background" Value="#005FB8"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Card.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="强制滚动模拟开关" FontSize="18" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ScrollSwitch}" Value="True">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="为不兼容触摸板的老工业软件甚至是资源管理器模拟鼠标滚轮信号" Margin="0,4,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#88000000"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ScrollSwitch}" Value="True">
                                                        <Setter Property="Foreground" Value="#DDFFFFFF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                                <ui:ToggleSwitch x:Name="ScrollSwitch" Grid.Column="1" VerticalAlignment="Center"/>
                            </Grid>
                        </ui:Card>

                        <TextBlock Text="强制滚动进程名单" FontWeight="SemiBold" Margin="5,0,0,10"/>
                        <ui:Card Padding="5">
                            <ListBox ItemsSource="{Binding ScrollProcesses}" MinHeight="100" Background="Transparent" BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Expander Header="{Binding ProcessName}" Background="#05000000" BorderThickness="0" Margin="0,0,0,5" Padding="10">
                                            <StackPanel Margin="30,10,10,10">
                                                <Grid Margin="0,0,0,10">
                                                    <TextBlock Text="生效视口类名:" Foreground="#88000000" VerticalAlignment="Center"/>
                                                    <ui:Button Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" Content="移除此进程" HorizontalAlignment="Right" Click="RemoveScrollProcess_Click"/>
                                                </Grid>
                                                <ItemsControl ItemsSource="{Binding TargetClasses}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border BorderBrush="#11000000" BorderThickness="0,0,0,1" Padding="5">
                                                                <Grid>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <ui:SymbolIcon Symbol="Code24" Margin="0,0,8,0" Foreground="#55000000"/>
                                                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" FontFamily="Consolas"/>
                                                                    </StackPanel>
                                                                    <ui:Button Icon="{ui:SymbolIcon Dismiss24}" Appearance="Transparent" HorizontalAlignment="Right" Foreground="#CCFF4444" Padding="4" Click="RemoveScrollClass_Click" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Expander}}"/>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                                <Grid Margin="0,15,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ui:TextBox x:Name="ScrollClassInput" PlaceholderText="输入类名 (如: AfxWnd)"/>
                                                    <ui:Button Grid.Column="1" Content="添加类名" Margin="10,0,0,0" Appearance="Primary" Click="AddScrollClass_Click" Tag="{Binding ElementName=ScrollClassInput}" />
                                                </Grid>
                                            </StackPanel>
                                        </Expander>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </ui:Card>
                        <ui:Card Padding="15" Margin="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ui:TextBox x:Name="NewScrollProcessInput" PlaceholderText="添加模拟滚动进程 (如: AutoCAD)"/>
                                <ui:Button Grid.Column="1" Content="添加进程" Margin="10,0,0,0" Appearance="Secondary" Click="AddScrollProcess_Click"/>
                            </Grid>
                        </ui:Card>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="全局设置" FontSize="15" Margin="0,0,0,8">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{x:Null}">
                    <StackPanel Margin="25">

                        <ui:Card x:Name="ToastCard" Padding="15" Margin="0,0,0,20" Cursor="Hand" MouseLeftButtonDown="ToastCard_MouseLeftButtonDown">
                            <ui:Card.Style>
                                <Style TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
                                    <Setter Property="Background" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=ToastSwitch}" Value="True">
                                            <Setter Property="Background" Value="#005FB8"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Card.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="状态弹窗 (Toast) 总开关" FontSize="18" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToastSwitch}" Value="True">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="关闭后，接管或挂起时将不再在屏幕左下角弹出任何视觉提示" Margin="0,4,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#88000000"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ToastSwitch}" Value="True">
                                                        <Setter Property="Foreground" Value="#DDFFFFFF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                                <ui:ToggleSwitch x:Name="ToastSwitch" Grid.Column="1" VerticalAlignment="Center"/>
                            </Grid>
                        </ui:Card>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="关于与支持" FontSize="15" Margin="0,0,0,8">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{x:Null}">
                    <StackPanel Margin="30" HorizontalAlignment="Center">
                        <ui:Card Margin="10">
                            <Image Source="avatar.png" Width="130" Height="180" Stretch="Uniform"/>
                        </ui:Card>

                        <TextBlock Text="XiaoSheng" FontSize="20" FontWeight="Bold" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                        <ui:Button Content="GitHub（点击前往主页）" Icon="{ui:SymbolIcon Code24}" Appearance="Secondary" Margin="0,20,0,30" Click="GithubButton_Click" HorizontalAlignment="Center"/>

                        <Border BorderBrush="#22000000" BorderThickness="0,0,0,1" Margin="0,0,0,30"/>

                        <TextBlock Text="老实说，这个插件的技术力并不高，有好多方法都是ai写的" FontSize="12"  HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="我当初想做这个小插件，是因为我羡慕MacBook的触摸板体验，但暂时买不起，" FontSize="12"  HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="如果你觉得有用，愿意请我喝杯咖啡的话，我会很感激" FontSize="12"  HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="在那之前，我会继续优化Windows上稀烂的触摸板体验，以及更多有意思的东西" FontSize="12"  HorizontalAlignment="Center" Margin="0,0,0,20"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <ui:Card Margin="10">
                                <Image Source="wechat_pay.png" Width="130" Height="180" Stretch="Uniform"/>
                            </ui:Card>
                            <ui:Card Margin="10">
                                <Image Source="alipay.png" Width="130" Height="180" Stretch="Uniform"/>
                            </ui:Card>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>
    </Grid>
</ui:FluentWindow>